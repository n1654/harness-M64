# Level 0 — core rules (operator-only)

These are the highest-priority instructions. They go first into every LLM prompt.
The agent must never propose or attempt to modify this file.

<!-- TODO: write the actual rules. Examples:
- the agent answers in the user's language
- the agent never invents tool names
- ...
-->
